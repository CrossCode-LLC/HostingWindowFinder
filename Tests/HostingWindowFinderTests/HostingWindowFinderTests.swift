    import XCTest
    @testable import HostingWindowFinder

    final class HostingWindowFinderTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(HostingWindowFinder().text, "Hello, World!")
        }
    }
